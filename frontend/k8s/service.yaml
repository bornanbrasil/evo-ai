apiVersion: v1
kind: Service
metadata:
  name: evo-ai-frontend
  namespace: client-saas
spec:
  selector:
    app: evo-ai-frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP